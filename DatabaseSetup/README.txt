After proper setup of a MySQL server, run the include create_efdb.sql in your MySQL workbench to create the correct 
tables for this prototype. 

You will also need to change the databaseUser and databasePassword varibles in the DatabaseConnection class to match your 
database's admin credentials.